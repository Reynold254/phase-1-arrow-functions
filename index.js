// Define the square function using an arrow function
const square = (x) => x * x;

// Define the add function using an arrow function
const add = (a, b) => a + b;

// Define the subtract function using an arrow function
const subtract = (a, b) => a - b;

// Define the multiply function using an arrow function
const multiply = (a, b) => a * b;

// Define the divide function using an arrow function
const divide = (a, b) => a / b;
